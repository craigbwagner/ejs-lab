<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %></title>
</head>
<body>
  <h1><%= name %></h1>
  <% if (isOpen === true) { %>
    <p>Yes we are Open!</p>
  <% } else { %>
    <p>Sorry, we are closed.</p>
  <% } %>
  <p><%= phone %></p>
  <p><%= address %></p>
</body>
</html>
