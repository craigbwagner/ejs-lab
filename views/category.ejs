<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><% category %></title>
</head>
<body>
  <%- include('./partials/nav') %>
  <h1><%= category %></h1>

  <h2></h2>
  <ul>
    <% dishes.forEach(item => { %>
      <li><%= item.name %> - $<%= item.price %> (<%= item.rating %>/5 stars)</li>
      <p><%= item.details %></p>
    <% }) %>
  </ul>
</body>
</html>
